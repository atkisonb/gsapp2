<%= form_for(@userfund) do |f| %>
  <% if @userfund.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@userfund.errors.count, "error") %> prohibited this userfund from being saved:</h2>

      <ul>
      <% @userfund.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :fund_id %><br>
    <%= f.number_field :fund_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
