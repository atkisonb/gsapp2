<%= form_for(@fund) do |f| %>
  <% if @fund.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fund.errors.count, "error") %> prohibited this fund from being saved:</h2>

      <ul>
      <% @fund.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fund_name %><br>
    <%= f.text_field :fund_name %>
  </div>
  <div class="field">
    <%= f.label :fund_description %><br>
    <%= f.text_area :fund_description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
